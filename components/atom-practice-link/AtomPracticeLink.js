import { LitElement, html } from "lit";
import { styles } from "./AtomPracticeLink-styles.js";

export class AtomPracticeLink extends LitElement {
  static properties = {
    
  }

  static styles = styles;

  constructor() {
    super();
  }
  
  render() {
    return html``;
  }
}