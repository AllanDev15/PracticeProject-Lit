import { LitElement, html } from "lit";
import { styles } from "./AtomPracticeIcon-styles.js";

export class AtomPracticeIcon extends LitElement {
  static properties = {
    
  }

  static styles = styles;

  constructor() {
    super();
  }
  
  render() {
    return html``;
  }
}